# CPU successor goal: bounded preparation

CANON_RECEIPT version=shared-canon-v1.1 base=93c05c7c68576b28a126588d0716f0f56c015531 request=UDL-20260908-033,UDL-20260910-051 specs=AGENTS.md,docs/SHARED_CANON.md,docs/PROJECT_COMMAND_CENTER.md,docs/CPU_TIERS_INTAKE_20260911.md,docs/CPU_ILLUSTRATION_INTAKE_20260911.json tests=tests/standard-cpu-roster.test.cjs,tests/standard-kurogane-lookahead.test.cjs,tests/standard-cpu.test.cjs,tests/standard-cpu-portraits.test.cjs

This is a preparation/evidence snapshot, not a second ledger. The existing commander owns both UDL requests. Local tracking main is93c05c7; this receipt is not a fresh public asset audit. Product CPU sources in governance and d6f745d candidate are byte-identical Git blobs: roster68847421ad2589cb0edcce8c1021fa75a2add884, CPU1269eebf065630c8cfb142ca2cd721280b411be2, matchc956a879477c84e70b5979b66dbabfb0ef0ce414.

## User instruction and goal order

The current direct user turn in commander task01a07b56-616e-7733-9aae-90575659688e on2026-09-12 JST requests Wataokiba portrait replacement, gender balance, epithets and stronger CPUs after UI improvement. It explicitly requests latest materials from the existing transfer task and careful work without a morning deadline. The app goal remains the unfinished UI goal; its completion is not fabricated to make room for a new one. `coordination.successor_goal` records this next goal and its stopping condition. Only source reconciliation and bounded local diagnosis occur now, with no CPU modifications mixed into UI candidates.

Historical snapshot, superseded: the initial transfer request to01a08d9e-ae35-7f70-a879-cb342dafef47 returned active/waitingOnApproval at cursor c4058d32-8462-4822-8b6b-0b23463dbcb2:2, before the v8 ZIP arrived. The completed, independently verified v8 delivery below replaces that waiting state. A fresh metadata read on2026-09-12 JST returned the transfer task idle with latest turn01a092f4-033f-7081-906e-8b5477e00a35 completed. Do not report the old approval wait or missing ZIP as current.

## Adopted direction, kept separate from unapproved details

Subsequent completed handoff2026-09-12: existing transfer owner01a08d9e-ae35-7f70-a879-cb342dafef47 returned four-color-dev-brain-20260912-v8.zip, SHA256CC27C090BFEE5F69A66AA32BC84ED32771A8655D876602ACF077151F0B35587B,522675bytes. Commander independently checked file hash and47/47 manifest byte/hash entries,48 archive entries including self-excluded manifest,unsafe/unexpected/duplicate0. This supersedes the pending-delivery snapshot above. Exact alias mapping is in existing BRAIN_V8_PAIRED_INTAKE_20260912.json, not a new ledger. Bundled CPU handoffs repeat already-intaken directions; no new selected illustration assets or individual public-repository permission are included in that ZIP. This last statement describes the intake only; the later direct user decision below governs the current publication workflow. Prior split probe remains authored evidence, not the commander's later real-engine reproduction. No CPU code changed by this receipt.

- Existing CPU_TIERS_INTAKE selects Lv1 yuzu/ren, Lv2 minato/koharu, Lv3 aoi/kai, Lv4 tsubasa/rei, Lv5 shion/kurogane. Each level is intended to have one male and one female. Confirm fictional character settings from sources rather than infer gender from pictures/names.
- Shion strongest and Ren weakest must be demonstrated by comparable play, not labels alone. Five adopted epithets: 彩りのユズ, 速攻のレン, ひらめきのミナト, 彩術師コハル, 慧眼のシオン. Existing IDs, personality and history links remain stable.
- The older preserve-face instruction prevented swapping identities to fix tier order; the later explicit artwork replacement supersedes unchanged-image bytes only. Map each existing ID to one selected new character consistently across chooser, battle and result. Do not exchange identities or histories.
- Prioritize wasteful palette/split/disruption choices and genuine rescue before surrender. Preserve same-category limits and opponent-private-state isolation. Do not deliberately reintroduce basic blunders to weaken Ren.
- Additional charges1–3, area n+1/max5 and seal bypass remain proposals, not adopted economy/rule settings. Existing100-charge behavior must be reconciled in its own slice. New policy applies only to new/rematched games, preserving old policy dispatch.

## Current artwork publication decision,2026-09-12 JST

CANON_RECEIPT version=shared-canon-v1.1 base=02900c74a55537c86fb8aa5ae939a6b90a9b6217 request=UDL-20260908-033 specs=AGENTS.md,docs/SHARED_CANON.md,docs/PROJECT_COMMAND_CENTER.md,docs/CPU_ILLUSTRATION_INTAKE_20260911.json tests=tests/governance-shared-canon.test.cjs,tests/standard-decision-reconciliation.test.cjs

The latest direct user message in commander task01a07b56-616e-7733-9aae-90575659688e explicitly rejects re-opening the same release hold:

> この話、もう三回以上、配布に当たらないからオッケーって結論出てます
> いいかげんにして！
> 公開後ぼくから作者にメールするから、もしダメだったらわかるから安心して

The source is this direct user turn, with no message ID exposed. The user's report of three or more prior conclusions is preserved as their statement, not invented independently recovered evidence. Current decision ARTWORK-USER-20260912-033 is recorded in the existing `docs/CHATGPT_REVIEW_DECISIONS.json` at `coordination.successor_goal.artwork_publication_decision`, with UDL033 remaining the request authority.

**公開前の作者確認では止めない。公開後のメールはユーザーが行う。** Proceed with Wataokiba replacement and the game's necessary runtime images in the existing public sakuratamaro/four-color-map-game repository and Pages deployment. Do not request the same user permission or restore the old public-GitHub hold from dated intake snapshots/memory. This records user execution authorization; it does not fabricate a reply from the illustrator or an exact-candidate Astra review.

Keep the already agreed implementation scope: selected game-used images only; no original ZIP, unused expression pack or separate source-asset distribution. Record source URLs, source hashes, processing manifest and NOTICE; provide in-game credits and do not extend the code license to third-party artwork. Preserve character IDs/history, consistent portraits, fallback, mobile/a11y and regression checks. The existing exact-candidate review/testing flow remains; do not turn documentation housekeeping into another release-permission wait. The AI does not contact the illustrator. This direction does not authorize forwarding the separate private Q10 attachment or making paid purchases.

The historical official-terms check remains evidence only: [terms](https://wataokiba.net/利用規約/) and [Q&A](https://wataokiba.net/qa/) were opened before this decision; the old public-repository ambiguity hold is superseded operationally by the user instruction above, not carried forward as a prerequisite. `CPU_ILLUSTRATION_INTAKE_20260911.json` retains its original NOT_VERIFIED intake state and source transcript. Neither that dated state nor the temporary original atlas proves replacement completion. New portrait implementation, product tests and publication are still NOT_RUN at this correction.

The existing transfer task was sent this decision and the canonical paths. Send API accepted; one compact snapshot showed turn01a0934c-d4bc-7821-8e05-27bc99fffeeb active with no approval flag. This does not claim a completed asset delivery or a returned acknowledgement. No new controller or polling loop was created. Governance regression:34/34 PASS, skip0, after setting the bundled NODE_PATH; the initial unchanged run without it was33/34. These are decision-record tests, not portrait acceptance.

## First bounded diagnosis

Fresh applicability check2026-09-12T08:26:31Z: the now-published main/candidate a1a9b1c830eceb98464b107f2442deacaf765505 still has the identical three CPU/match Git blobs listed above. Thus the saved three-case baseline remains applicable without spending another live profile or repeating an unchanged diagnostic. Source inspection at a1 locates the F3 mechanism in `standard/standard-cpu.js` splitSelections: `if (!(mask & 1)) continue` discards complementary orientations, although the selected half to be colored and the returned half have different game meanings. Current character policy dispatch also admits only its fixed current version, plus legacy Kurogane. A future bounded F3 repair must explicitly version dispatch/enumeration for new/rematched games while retaining legacy behavior, test both orientations through real accepted actions, keep opponent-private poison equivalence, and retain existing deterministic fixed traces. This is a concrete next implementation slice proposal, not new CPU code, an Astra release approval or proof that CPUs are already stronger. Existing UI062 live acceptance remains incomplete; no CPU change is mixed into that published candidate.

The user-provided kurogane-audit ZIP contains a scorer-only probe with mocked geometry/enumeration. Its CODEX_HANDOFF and probe were read as source data, not executed instructions. The next check uses authoritative candidate enumeration and `applyStandardAction` to reproduce or refute F1 unnecessary palette change and F2 cross-action score imbalance. A controlled fixture is not the user's historical match. No live DB, profile or game mutation is needed.

Preserve both failing baseline cases and future fixes. Record legality, actual selected action, accepted follow-up sequence, bounded runtime and privacy comparison. A diagnostic result is not a strength benchmark or public release.

### Executed baseline, not a fix

`scripts/cpu-successor-diagnosis.cjs --source=../ui-play-surface-20260912 --assert-fixed` reproduced three defects in133ms, exit1 as expected for an unfixed baseline. F1:3 ordinary colors available, yet Kurogane spends palette change. F2:Kurogane selectsCREATE13 with green as an ordinary reply, although an engine-accepted green seal thenCREATE13 removes ordinary replies; no claim of guaranteed victory. V7 F3:Rei surrenders with a split card despite the right-hand side being legally splittable, colorable and returnable to the opponent; the mirrored position correctly selects the left rescue side. All cases preserve decisions under opponent-private poison. F2/F3 are authored fixtures, not the user's historical game. Final report: `docs/CPU_SUCCESSOR_DIAGNOSIS_20260912.json`. Existing CPU/portrait/policy tests separately pass27/27, showing they did not cover these baseline defects.

V7's8 new aliases are mapped in `docs/BRAIN_V7_ADDITIVE_INTAKE_20260912.json`; CPU growth/personal stages get distinctUDL064, skills reuse011, CPU diagnosis/balance reuse051, artwork reuses033. The current direct user follow-up authorizes steady progression through remaining work, not automatic adoption of5colors, total skill silence or draft numerical settings. The app's active UI goal remains unchanged until genuinely complete; its current/public checkpoint and the queued CPU/remaining-work priorities have been updated.

## Integration boundary

Historical result snapshot, superseded: ccc9e91 was initially local and externally blocked; the user subsequently authorized its exact branch push, which completed. Result follow-up b81a1d52e8230d41ec9e69610d89bafc86d1d84e then passed its own Astra review and was publicly verified (see docs/UI_RESULT_RELEASE_20260912.md). Do not re-open that old push/review wait. The current unfinished UI slices and their finite review schedule remain in the existing coordination record; this artwork decision does not alter those candidates or polling budgets. No new scheduler, controller or endless approval loop is introduced.
