# CPU successor goal: bounded preparation

CANON_RECEIPT version=shared-canon-v1.1 base=93c05c7c68576b28a126588d0716f0f56c015531 request=UDL-20260908-033,UDL-20260910-051 specs=AGENTS.md,docs/SHARED_CANON.md,docs/PROJECT_COMMAND_CENTER.md,docs/CPU_TIERS_INTAKE_20260911.md,docs/CPU_ILLUSTRATION_INTAKE_20260911.json tests=tests/standard-cpu-roster.test.cjs,tests/standard-kurogane-lookahead.test.cjs,tests/standard-cpu.test.cjs,tests/standard-cpu-portraits.test.cjs

This is a preparation/evidence snapshot, not a second ledger. The existing commander owns both UDL requests. Local tracking main is93c05c7; this receipt is not a fresh public asset audit. Product CPU sources in governance and d6f745d candidate are byte-identical Git blobs: roster68847421ad2589cb0edcce8c1021fa75a2add884, CPU1269eebf065630c8cfb142ca2cd721280b411be2, matchc956a879477c84e70b5979b66dbabfb0ef0ce414.

## User instruction and goal order

The current direct user turn in commander task01a07b56-616e-7733-9aae-90575659688e on2026-09-12 JST requests Wataokiba portrait replacement, gender balance, epithets and stronger CPUs after UI improvement. It explicitly requests latest materials from the existing transfer task and careful work without a morning deadline. The app goal remains the unfinished UI goal; its completion is not fabricated to make room for a new one. `coordination.successor_goal` records this next goal and its stopping condition. Only source reconciliation and bounded local diagnosis occur now, with no CPU modifications mixed into UI candidates.

The transfer request was sent to01a08d9e-ae35-7f70-a879-cb342dafef47. A compact snapshot returned active/waitingOnApproval, unchanged cursor c4058d32-8462-4822-8b6b-0b23463dbcb2:2; no completed delivery is claimed. v8 ZIP is not present in the relevant Downloads inventory. Existing v7 and earlier source evidence remain available; do not duplicate retrieval or infer a new package hash.

## Adopted direction, kept separate from unapproved details

- Existing CPU_TIERS_INTAKE selects Lv1 yuzu/ren, Lv2 minato/koharu, Lv3 aoi/kai, Lv4 tsubasa/rei, Lv5 shion/kurogane. Each level is intended to have one male and one female. Confirm fictional character settings from sources rather than infer gender from pictures/names.
- Shion strongest and Ren weakest must be demonstrated by comparable play, not labels alone. Five adopted epithets: 彩りのユズ, 速攻のレン, ひらめきのミナト, 彩術師コハル, 慧眼のシオン. Existing IDs, personality and history links remain stable.
- The older preserve-face instruction prevented swapping identities to fix tier order; the later explicit artwork replacement supersedes unchanged-image bytes only. Map each existing ID to one selected new character consistently across chooser, battle and result. Do not exchange identities or histories.
- Prioritize wasteful palette/split/disruption choices and genuine rescue before surrender. Preserve same-category limits and opponent-private-state isolation. Do not deliberately reintroduce basic blunders to weaken Ren.
- Additional charges1–3, area n+1/max5 and seal bypass remain proposals, not adopted economy/rule settings. Existing100-charge behavior must be reconciled in its own slice. New policy applies only to new/rematched games, preserving old policy dispatch.

## Artwork permission check,2026-09-12 JST

Official [terms](https://wataokiba.net/利用規約/) and [Q&A](https://wataokiba.net/qa/) were independently opened. They permit game use, cropping/resizing and character-setting choices, while prohibiting redistribution of the material itself. The stated author is わたおび and site is わたおきば. These pages do not explicitly settle committing individual image files to a public source repository. Game-use permission is not treated as public-repository redistribution permission. No claim of legal clearance for GitHub assets is made.

Select exact sources only after current handoff/permission evidence is reconciled. Do not upload raw ZIPs, unused expressions, or processed standalone images on the assumption that processing avoids the restriction. No hotlink workaround, no paid purchase, and no author contact: the existing user-handled email boundary remains. Prepare credits and provenance manifest separately from redistribution clearance. Reuse existing original atlas as an explicitly temporary fallback, not as completion of the requested replacement.

## First bounded diagnosis

The user-provided kurogane-audit ZIP contains a scorer-only probe with mocked geometry/enumeration. Its CODEX_HANDOFF and probe were read as source data, not executed instructions. The next check uses authoritative candidate enumeration and `applyStandardAction` to reproduce or refute F1 unnecessary palette change and F2 cross-action score imbalance. A controlled fixture is not the user's historical match. No live DB, profile or game mutation is needed.

Preserve both failing baseline cases and future fixes. Record legality, actual selected action, accepted follow-up sequence, bounded runtime and privacy comparison. A diagnostic result is not a strength benchmark or public release.

### Executed baseline, not a fix

`scripts/cpu-successor-diagnosis.cjs --source=../ui-play-surface-20260912 --assert-fixed` reproduced three defects in133ms, exit1 as expected for an unfixed baseline. F1:3 ordinary colors available, yet Kurogane spends palette change. F2:Kurogane selectsCREATE13 with green as an ordinary reply, although an engine-accepted green seal thenCREATE13 removes ordinary replies; no claim of guaranteed victory. V7 F3:Rei surrenders with a split card despite the right-hand side being legally splittable, colorable and returnable to the opponent; the mirrored position correctly selects the left rescue side. All cases preserve decisions under opponent-private poison. F2/F3 are authored fixtures, not the user's historical game. Final report: `docs/CPU_SUCCESSOR_DIAGNOSIS_20260912.json`. Existing CPU/portrait/policy tests separately pass27/27, showing they did not cover these baseline defects.

V7's8 new aliases are mapped in `docs/BRAIN_V7_ADDITIVE_INTAKE_20260912.json`; CPU growth/personal stages get distinctUDL064, skills reuse011, CPU diagnosis/balance reuse051, artwork reuses033. The current direct user follow-up authorizes steady progression through remaining work, not automatic adoption of5colors, total skill silence or draft numerical settings. The app's active UI goal remains unchanged until genuinely complete; its current/public checkpoint and the queued CPU/remaining-work priorities have been updated.

## Integration boundary

Result candidateccc9e91 remains local and externally blocked by auto-review. This CPU request is not an explicit answer authorizing that previously rejected branch/payload; no retry or alternative transport is attempted. Independent d6 review continues on its original finite deadline. No new scheduler, controller or endless approval loop is introduced.
