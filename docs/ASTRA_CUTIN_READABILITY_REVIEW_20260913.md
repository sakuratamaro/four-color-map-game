# Genuine Astra review040

Source: completed agent message `fb1e2e48-53e5-4a1a-b34a-2fa2e66f862d` paired with exact4443character request `3e6efd53-710a-45ed-9de4-05627ca5f40e` in existing ChatGPT task `6aa229e7-e098-83ee-ac5e-d366a12653a4` (`改修ロールバック防止策`). Retrieved once at the original first20-minute slot, read reserved2026-09-13T01:10:11Z, completed response5467characters. Source below is verbatim, not a Codex-authored approval.

## DECISION

**APPROVE_RELEASE — 固定候補954eのPages-only公開を承認します。CPU候補9590の公開を待たず、このUIを先に公開して構いません。**

今回取得したGitHubの状態では、**954eのWindowsは既に両ブラウザーとも成功しています。** 提出時の`IN_PROGRESS`から進んでおり、この候補についてCI待ちを続ける必要はありません。公開直前のmain再照合、同一SHAのPages成功、配信byte確認は引き続き必要です。

```text
DECISION: APPROVE_RELEASE
SUBJECT_SHA: 954e1c5c52d5453fc9fee9872b2d7e922f850a39
BASE_SHA: d9ce111d7d97019d55b3e90842602001e045ea04
CANON_VERSION: shared-canon-v1.1
FEATURE_SPEC_VERSION: UDL-065-readability-v1.1
SPEC_PATH: docs/SKILL_CUTIN_READABILITY_20260913.md
SPEC_BLOB: 2af5d73528a35edc29f45b3be5b700f6ceaf4688
SCOPE: Pages_only
REVIEW_KIND: game_production_release_approval
DB_CHANGE_SET: []
EDGE_CHANGE_SET: []
MANAGED_SETTING_CHANGE_SET: []
BLOCKERS: []
```

固定仕様の版・blobは取得結果と一致しています。承認対象はこの候補と変更セットであり、CPU039、別の運用文書差分、後続の相手技名公開イベントを含みません。

## BLOCKERS

**今回読んだ固定差分について、公開を止めるコード・仕様上の問題は認めません。**

Windows run `34728306768`、attempt 1、head SHA `954e1c5…`の取得結果は次のとおりです。

| 対象 | Job ID | 最終結果 | 完了時刻・UTC |
|---|---|---|---|
| Chrome | `103646336438` | `completed / success` | 2026-09-13 00:45:50 |
| Edge | `103646336560` | `completed / success` | 2026-09-13 00:48:30 |

run全体も`completed / success`です。Chromeのlocal lifecycleは既存構成による`skipped`、Edgeの同段階は`success`なので、**「全workflow skip 0」とは記録しないでください。** この成功はあくまで954eのもので、CPU9590の失敗を解消するものではありません。

新liveハーネスの未実装・未検証は、**live実行前に満たす条件**として残します。Pages公開そのものを、その準備完了待ちにする必要はありません。

## NOTES

### 1．表示時間だけでなく、「更新で読み時間が消える問題」に対応できています

固定パッチは分割取得して最終hunkまで読みました。加えて、固定SHAの仕様、`skill-cutin.js`全文、`app.js`の表示・中断・観測処理を確認しています。

今回の変更では、`DISPLAY_MS = 1800`をJSの消去タイマーとCSSへ共通で渡し、通常アニメーションの10～90％を静止区間にしています。**1.8秒を今回の設計値として採用する判断は妥当です。** ただし、ユーザー指定値や「誰でも必ず読み切れることを確認した値」には読み替えません。

特に、連続する通常更新では表示済みカードを消さず、同時に`generation`を進めて**未表示の遅延claimだけを無効化する**処理を評価します。通常更新でタイマーを設定し直していないため、読み時間を保持しながら無期限延長を避けています。新しい技では旧表示を解除し、非表示状態のレイアウトを確定してから再表示するため、カードのCSSアニメーションも再開始する構造です。

表示中の塗り操作、通常更新後の同一イベント維持、新しい技による置換と時間の再開始は、追加されたブラウザーテストにも対応しています。今回の時間assertionは、旧仕様の時間判定を単に緩めるだけでなく、**表示開始から消去までの実測に下限・上限を設ける変更**になっています。

なお、実測1800～1810msはその試験条件での記録です。ブラウザーのタイマーは指定より遅れて実行され得るため、全環境で厳密に1800msになる保証とはしません。citeturn875319search0

### 2．結果説明の範囲と、相手技名の未実装を分けた扱いを承認します

結果説明は、閲覧者本人の持ち色・封印と公開盤面の差分に限定されています。持ち色は順序・重複を正規化し、隣接条件から「塗れる色が増えた」と推測する処理はありません。空振りも、同じscope・event IDに対応する自分のACKでのみ確定し、不明な効果は中立表現に残しています。

**「相手の技名が取得できないから、相手の秘密情報から推測して埋める」方向へ進めない判断は正しいです。** 今回は「読み時間の改善＋確認できた結果の一言説明」として公開し、相手技名は同じUDL065の未達項目として残してください。汎用タイトルのまま公開することは認めますが、それをもって元の「使った技・結果を一言」という要望全体の完成にはしません。

また、表示中に後続の通常操作が行われても、この説明は**直前に起きた出来事の説明**です。「現在もその持ち色・盤面のままである」という保証として扱わない、という仕様上の区別を維持してください。

### 3．UI先行公開の条件

今回取得した`main`は、申告どおり`d9ce111d7d97019d55b3e90842602001e045ea04`でした。ただし、これは取得時点の確認であり、公開直前の再確認を代替しません。

公開は次の3段階で進めてください。

1. **公開直前にfresh mainがd9ceのまま、候補が正確な954e・cleanであることを確認する。** 他の公開処理と競合してmainが変わっていれば停止し、新base・新候補として再照合する。
2. **forceなしでmainを954eへ進め、同じ954eのPages成功を確認する。** この便のためのDB適用・Edge配備・管理設定変更は行わない。
3. **fresh candidate preflightと、HTML／app43／cut-in JS1／cut-in CSS1の厳密byte一致を確認する。** バージョン文字列が表示されているだけで、byte一致の代用にしない。

対象アセットは`index.html`、`app.js?v=20260913-43`、`skill-cutin.js?v=20260913-1`、`skill-cutin.css?v=20260913-1`です。固定差分ではpreflightにも新しいcut-inのJS／CSS marker確認が追加されています。

**上記が揃えば、同じ954eについて公開許可を取り直す必要はありません。** CPU先行という以前の作業予定は解除してよく、今回のUIを独立して公開してください。

その後のCPU統合は、提案どおりfresh base上の新SHA・必要なテスト・新レビューへ進めます。CPU039の条件付き承認は新しい統合対象へ流用せず、逆に、このUIの成功をCPUの失敗ジョブの代替にも使いません。

### 4．新UI liveは、提案された有限枠で承認します

**今回の954e専用として、下記の一回だけを認めます。** ハーネスが既に実装・検証済みという承認ではなく、固定製品を変更せず、この制限を実装・局所検証してから実行する条件です。

| 項目 | 承認する上限・範囲 |
|---|---|
| 対象 | 新規匿名プロフィール1件・ユズ対局1件 |
| 試行 | 1回。再実行・再戦なし |
| 全体時間 | 最初の本番mutation直前から、終了処理を含む共通240秒 |
| 通常プレイ | 150秒で打切り。残り90秒を終局・独立読戻し・teardownに確保 |
| CPU操作 | ブラウザー／API合算で、送信試行最大8回 |
| 自分の通常ゲーム操作 | 送信試行最大6回 |
| 投了 | 上記6回とは別に、SURRENDER送信最大1回 |
| 再送 | 0回。結果不明時も追加送信で解決しない |

この枠は提出仕様の提案に対応するもので、旧065・067・F3・CPU039の枠を移したものではありません。

実行条件は次のとおりです。

**試行予約と送信制御。** 最初のmutation前に候補SHAへ結び付けた排他的な試行予約を永続化し、プロセス再起動でも再実行できないようにすること。CPUと自分の操作は、ハーネスの操作ループだけでなく、**ブラウザー側の自動送信を含む実送信前の境界**で計数・制限してください。上限に達してからログで発見する実装では不十分です。

ブラウザーの通信制御にPlaywrightのrouteを使う場合は、Service Workerにより捕捉できない経路も考慮してください。公式資料でも、その用途では`serviceWorkers: 'block'`が案内されています。これはハーネス側の実装上の注意で、製品への新機能追加を求めるものではありません。citeturn875319search3

**終局優先。** 通常プレイの時間・操作上限に達したら、新たな技の観測を狙わず終了経路へ進むこと。投了前に終局状態を読み、既に終了していれば送信しないでください。結果不明のCPU操作を抱えたまま別操作を重ねず、読戻しを優先します。SURRENDER送信後の応答喪失も再送せず、終局snapshot-v2と本人プロフィールを独立して確認してください。

**取得範囲。** 現行snapshot-v2の本人membership・対局・seat・version整合性を検査し、finishedの読戻しに`initialize`を使わないこと。CPU秘密情報、クイズ・ガチャ・売買、直接DB操作、特権回復、状態注入は範囲外です。トークンや対局等の生IDを共有報告へ出さず、失敗記録も安全な段階名・HTTPコード等で残してください。

**局所検証。** 上限超過、150秒打切り、共通240秒期限、送信結果不明、既存終局、snapshot不一致、試行予約済みでの再起動を検査してから実行すること。検証用ハーネスだけの準備・証拠保存であれば、新しい製品SHAを作る必要はありません。製品へ変更を入れる場合は今回の承認対象外です。

### 5．自然に技が出なかった場合と、受入範囲

**技が自然発生しなければ`NOT_OBSERVED`で終了してよく、追加対局・追試は認めません。** ただし、次の区別は必要です。

| 実際に起きたこと | 記録する扱い |
|---|---|
| 対象の技が発生しなかった | 該当する自分／相手の表示検査を`NOT_OBSERVED` |
| 技は発生したが、仕様どおりモーダル・終局等で抑止／中断された | 抑止・中断理由を記録。通常表示時間の検証とは分ける |
| 表示条件を満たす技を観測したのに表示されない、説明が誤る、操作を妨げる | `FAIL`。`NOT_OBSERVED`へ置き換えない |

ゲーム進行・終局・本人精算が成功しても、**それだけでカットインの本番表示まで検証済みにしないでください。** 自分側と相手側、表示内容と表示時間を分け、観測できた項目だけを受け入れる扱いです。正常な投了による敗北数・戦績・通常報酬の更新は、許可した対局の通常精算として確認し、それ自体を禁止された別の経済操作とは扱いません。

976/976、局所Chrome／Edge各7/7、4枚のスクリーンショット目視は、固定されたCodex実施記録として受領しています。こちらでは全テストの再実行や、そのローカルPNGの再目視はしていません。今回独立に確認したのは、固定差分・主要ソース・仕様blob・GitHubの最終CI結果・取得時点のmainです。

**この返答の受領後は本レビュー待機を閉じ、954eの公開直前照合へ進めてください。** 相手技名の後続、067の部分受入、CPU039の原失敗・別承認、物理端末`NOT_RUN`、別系統の文書導入は、それぞれ現在の区分を維持します。
