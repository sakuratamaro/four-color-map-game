# UDL060 terminal hierarchy and three actions

CANON_RECEIPT version=shared-canon-v1.1 base=87f12c0a2da3da3563824d22a2e3c1dc2c645c90 request=UDL-20260912-060,ADD-20260912-TERMINAL-HIERARCHY-THREE-ACTIONS specs=docs/SHARED_CANON.md,docs/BRAIN_V13_DELTA_INTAKE_20260912.json tests=tests/standard-result-continuation.test.cjs,tests/standard-online-browser.test.cjs worktree=.codex-worktrees/ui-terminal-hierarchy-20260914

Specification version: UDL-060-terminal-v2. Existing commander owner: 01a07b56-616e-7733-9aae-90575659688e. Source: existing ChatGPT 6aa229e7-e098-83ee-ac5e-d366a12653a4, user message bbb2137d-8aaa-4432-ab40-0938b5a4cde2; reconciled v13 alias above. The delivered v13 intake record and SCREENSHOT_UI_REFINEMENT_HANDOFF.md confirm the same adopted requirement. Its Lv.2 / two-ticket text is an example, not a new reward rule.

## Adopted outcome

- Win/loss is the largest element; CPU dialogue/art follows; reason is smaller; reward shows only the actually saved ticket level and count.
- Remove the routine saved-stat confirmation and ticket before/after numbers from the result celebration. Pending, failure, lab and reward-limit states remain short and truthful.
- The overlay offers at most three actions: ガチャへ, 結果を閉じてロビーへ, 対戦結果へ戻る. It does not offer direct rematch or another CPU. The persistent result must not reintroduce those shortcuts; a new opponent is selected from the lobby.
- Gacha navigation uses the saved level without drawing; lobby close is client-only and explicit; return closes only the overlay and preserves the room. Existing pending/rematch recovery and server/client protocol remain intact.
- Preserve replay suppression, own-seat reward/private boundaries, actual terminal reason, keyboard focus, small-screen/landscape reachability and reduced motion.

The persistent result shows a rematch control only to recover an already persisted request; it cannot initiate a fresh rematch. The separate existing post-draw gacha continuation is not changed in this terminal-only slice. Its relation to the broad lobby-first preference must be explicit in the genuine review, not silently counted as migrated here.

## Separate preserved work

This is a local UI-only slice on approved but unpublished parent87. It contains no new image bytes, CPU policy/roster/SQL/Edge changes or production operations. Candidate21cc31a's 20-image replacement and fullbody component remain untouched, as do8dd9 and the closed parent Pages/review budgets. Final artwork and this layout need combined verification after authorized integration; existing generated portraits do not prove final-art acceptance. A new candidate needs its own Windows and genuine Astra review; no old approval is inherited.

## Acceptance evidence to obtain

Executable cases must cover saved rewards at all five levels, wins/losses/CPU/PvP, spent tickets, missing or mismatched receipts, labs and limit/no-reward states. Browser checks must count the applicable actions, distinguish all three destinations, verify no action/draw/settlement replay, keep a pending rematch recoverable and show visual hierarchy at390/1280 and short landscape. Previously public060 records and failed tests remain historical, not new acceptance.
