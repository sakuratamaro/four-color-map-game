# Storage adapter

Local persistence is intentionally deferred. Planned first adapter: `expo-sqlite` for card inventory, quiz history, settings and local match history.

Do not store game-rule state mutation logic in this layer.
